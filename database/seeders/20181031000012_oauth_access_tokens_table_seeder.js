'use strict'

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // await queryInterface.bulkInsert('oauth_access_tokens', [], {})
  },

  down: async (queryInterface, Sequelize) => {
    // await queryInterface.bulkDelete('oauth_access_tokens', null, {})
  }
}
