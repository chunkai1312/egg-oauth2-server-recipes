'use strict'

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // await queryInterface.bulkInsert('oauth_authorization_codes', [], {})
  },

  down: async (queryInterface, Sequelize) => {
    // await queryInterface.bulkDelete('oauth_authorization_codes', null, {})
  }
}
